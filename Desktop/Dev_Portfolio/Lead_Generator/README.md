# 🕵️‍♂️ Lead Generator Bot (Web Scraper)

## 📌 Overview
This is a Python-based automation tool designed to extract commercial data from e-commerce websites. It navigates through product pages, captures key details (Titles and Prices), and exports the data into a clean CSV (Excel) file for business analysis.

## 🚀 Features
* **Automated Extraction:** Scrapes product names and pricing in seconds.
* **Data Export:** Automatically generates a `leads.csv` file compatible with Excel and Google Sheets.
* **Error Handling:** Includes robust logic to handle connection issues.

## 🛠️ Technology Stack
* **Python 3.10+**
* **BeautifulSoup4** (for parsing HTML)
* **Requests** (for HTTP sessions)
* **CSV Module** (for data management)

## ⚙️ How to Run
1.  Install the dependencies:
    ```bash
    pip install beautifulsoup4 requests
    ```
2.  Run the script:
    ```bash
    python main.py
    ```
3.  Open the generated `leads.csv` file to view the data.

---
*Built by [Your Name] - Electrical & Telecommunication Engineering Student*